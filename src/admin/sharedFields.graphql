fragment AdminMemberInfo on AdminPortalMember {
  earliestTimeline {
    ...TimelineEntry
  }
  latestTimeline {
    ...TimelineEntry
  }
}

fragment AdminMemberDetails on AdminPortalMember {
  ...AdminMemberInfo
}

fragment TimelineEntry on AdminTimelineEntry {
  providerId
}
